# DeepSeek-OCR 환경 설정 예제
# 이 파일을 .env로 복사하고 환경에 맞게 수정하세요

# ==========================================
# 디바이스 설정
# ==========================================
# cpu: CPU 사용 (느림)
# mps: Apple Silicon (M1/M2/M3) GPU
# cuda: NVIDIA GPU (Linux/Windows)
DEEPSEEK_DEVICE=cpu

# ==========================================
# 데이터 타입
# ==========================================
# float32: 정확도 우선 (M1 Mac MPS 권장)
# float16: 메모리 절약 (CUDA 권장, RTX 4060/4090)
# bfloat16: 안정성과 속도 균형 (CUDA 지원 시)
DEEPSEEK_DTYPE=float32

# ==========================================
# 배치 크기
# ==========================================
# 1: 메모리 8GB 이하 (M1 Mac, RTX 4060)
# 2-4: 메모리 16GB 이상 (RTX 4090)
DEEPSEEK_BATCH_SIZE=1

# ==========================================
# 모델 경로
# ==========================================
# 모델은 자동으로 HuggingFace에서 다운로드됩니다
# 또는 수동으로 ./models/DeepSeek-OCR/ 에 저장
DEEPSEEK_MODEL_PATH=./models/DeepSeek-OCR

# ==========================================
# 출력 디렉토리
# ==========================================
DEEPSEEK_OUTPUT_DIR=./outputs

# ==========================================
# 로깅 설정
# ==========================================
# DEBUG: 상세 로그
# INFO: 일반 정보
# WARNING: 경고만
# ERROR: 오류만
LOG_LEVEL=INFO
